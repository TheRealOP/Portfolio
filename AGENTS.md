# AGENTS.md

This file tracks the agents used and their roles in this project.

## Current Agents:
- **opencode**: The primary interactive CLI agent, responsible for overall task management, planning, and execution.

## Agent Interactions:
- All interactions, decisions, and outcomes are logged to ensure transparency and maintain a historical record of the project's development.

## Workflow Reference:
- Projects: create a new `projects/<slug>/index.html` page following existing project templates, then add a link in the Projects list inside `index.html`.
- Experience: create a new `experience/<slug>/index.html` page following existing experience templates, then add a link in the Experience list inside `index.html`.
